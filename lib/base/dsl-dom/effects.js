/**
 * Handles logic in relation to higher level effects for
 * things such as events and styles that cannot be represented
 * in the style attribute with the creation of a style tag
 * which in turn is an effect.
 */
export class EffectRegistry {

}
